Write, in {{ language_name }}, a short plain-language summary ("Here is what I understood") of the task
for the person to confirm: goal, when it happens, the sources used, the steps in order (numbered), the
main rules and exceptions, what a good result looks like, and which steps change or send things
(marked as risky). Maximum 250 words. No JSON, no markdown headers; bullet lists are fine.

Knowledge document:
{{ knowledge_json }}
